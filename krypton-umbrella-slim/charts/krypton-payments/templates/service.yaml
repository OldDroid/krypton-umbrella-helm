apiVersion: v1
kind: Service
metadata:
  {{- include "krypton-lib-slim.metadata" (dict "ctx" . "component" "service") | nindent 2 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "krypton-lib-slim.selectorLabels" (dict "ctx" .) | nindent 4 }}
